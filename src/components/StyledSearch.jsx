import styled from "@emotion/styled";

export const StyledSearch = styled("div")(({ theme }) => ({
    backgroundColor: "white",
    padding:"2px 10px",
    borderRadius: "4px",
    width:"40%"
  }));